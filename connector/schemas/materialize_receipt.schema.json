{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/aoa-telegram/materialize_receipt.schema.json",
  "title": "AoA Telegram Materialize Receipt",
  "type": "object",
  "required": ["schema", "run_id", "profile_id", "network_touched", "read_only"],
  "properties": {
    "schema": {
      "enum": [
        "aoa_telegram_materialize_receipt_v1",
        "aoa_telegram_desktop_export_materialize_receipt_v1",
        "aoa_telegram_mtproto_materialize_receipt_v1",
        "aoa_telegram_source_sync_receipt_v1"
      ]
    },
    "run_id": { "type": "string" },
    "profile_id": { "type": "string" },
    "network_touched": { "type": "boolean" },
    "read_only": { "const": true }
  },
  "additionalProperties": true
}
